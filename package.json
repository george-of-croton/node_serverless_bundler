{
  "name": "node_serverless_bundler",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d build",
    "build:watch": "babel src -w -d build"
  },
  "author": "",
  "license": "ISC",
  "bin": {
    "bundle": "build/index.js"
  },
  "dependencies": {
    "babel-core": "^6.26.0",
    "babel-plugin-shebang": "0.0.0",
    "babel-preset-es2015": "^6.24.1",
    "child-process": "^1.0.2"
  }
}
